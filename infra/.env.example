# Optional: point the web app at the FastAPI demo provider.
# If unset (or unreachable), the app runs on the in-browser deterministic
# demo provider and shows a "demo-provider" badge. No secrets exist in Task 001.
NEXT_PUBLIC_API_URL=http://localhost:8000
