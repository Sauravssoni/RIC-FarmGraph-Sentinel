# Demo Script — 12 Steps (mirrors `/demo`)

**Setting:** one laptop, no internet required. Reset at `/demo` first (deterministic — same numbers every run). Persona switcher top-right; language toggle हिंदी/EN.

1. **Command centre** — Open `/command-centre`. *Judges notice:* KPI cards drill into filtered registers; every screen carries the demo banner; map is licence-clean with an honesty caption.
2. **The field** — `/field/scan`. Toggle **Simulate offline**. Consent screen first (channel + purpose). Voice affordance present with typed fallback — *say it plainly:* no fake ASR.
3. **First capture fails** — golden farmer `RJ-DEMO-F1042`, bajra, vegetative, *pale streaking*; tick only "close-up of affected leaf". Quality gate fails, case saved **offline** with `NEEDS_RECAPTURE`; pending-sync chip = 1. *Judges notice:* the system refuses bad evidence instead of digesting it.
4. **Guided recapture** — recapture on the same case with close-up + lower-leaf + whole-plant. Gate passes (coverage 0.85). Sync now → chip drains to 0, audit event written.
5. **Simulated triage** — candidates **0.62 / 0.27 / 0.11**, margin 0.35 < 0.40 → routes to expert. *Judges notice:* the "Simulated score" label and reasons/missing-evidence panel — honesty as a feature.
6. **Expert queue** — `/expert` (switch persona). Golden case waits with priority reason; nearby compatible cases shown. Confirm downy mildew with a note.
7. **Outbreak strengthens** — `/outbreaks`: **CL-2601 re-scores 65.5 → 71.5, SUSPECTED → VERIFIED**; component bars and explanation visible. Also show **CL-2602 DISMISSED** — duplicate penalty on display.
8. **Mission** — generate from CL-2601; representative order = unverified-first then nearest; one-open-mission guard demonstrated by clicking again.
9. **Advisory** — issue approved `ADV-2601-v0.3`; Do-now / Monitor / Escalate columns; **chemical section LOCKED** in dashed red. *Judges notice:* version history on `/governance`.
10. **Follow-up** — record "improving"; case trends to outcome. Show the append-only timeline — every step, actor-marked `(demo)`.
11. **Governance** — model registry ("no accuracy measured — none claimed"), advisory lifecycle board, audit stream with type filter.
12. **Integrations truth-telling** — `/integrations`: 17 adapters, **none live**, each with status, consent basis, fallback. Close: *"Everything you saw runs offline, resets deterministically, and never claims what it hasn't measured."*

**Reset** — `/demo` → Reset demo (also resets the API if connected). Re-run any step; numbers are identical.
